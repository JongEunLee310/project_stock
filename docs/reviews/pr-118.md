# 로컬 리뷰: PR #118 — 의사결정 저널(decision-log) 도메인 신설 (#102)

리뷰어: Claude Code (Opus, BE 트랙). 구현: Codex. 판정: **머지 가능(사람 최종 승인·머지 대기)**.

## 범위 대조

`docs/designs/decision-log-domain.md` §6(계약 확정)·`.codex/tasks/task-102` 핸드오프와 1:1 대조. 범위 내 변경만 존재, 범위 밖(forward-only 전이·스냅샷 typed·검색필터·FE) 미포함 확인.

## 정확성

- **엔드포인트**(§6.1): `GET 목록`(페이지네이션·sort)·`POST`·`GET {id}`·`PATCH {id}` 전부 `get_current_user` 인증. ✔
- **소유권**: `_get_owned_decision_log`가 미존재 404(`DECISION_LOG_NOT_FOUND`)·타인 403(`DECISION_LOG_FORBIDDEN`) — watchlists 패턴 일치. ✔
- **라이프사이클 스탬프**(§6.3): `_with_lifecycle_stamp`가 `→REVIEWED`/`→CLOSED` 시 해당 타임스탬프가 null이고 명시 제공이 없을 때만 `now()` 스탬프. `model_dump(exclude_unset=True)`로 명시값 우선 보장. ✔
- **직렬화**: `target_price`/`stop_loss_price` Pydantic v2가 Decimal→문자열로 직렬화(테스트 `"220.0000"` 단언으로 확인). datetime은 `UtcDatetime`(`...Z`). snake_case. ✔
- **enum**(§6.4): 영문 UPPER_SNAKE 3종, repository에서 `.value`로 저장(enum 객체 누수 없음). 잘못된 값 422. ✔
- **테이블**(§6.2): 마이그레이션 컬럼·타입·server_default·FK가 설계와 정확히 일치(실DB `\d` 대조). ✔

## 검증 (독립 재실행)

- `ruff check .` 통과 · `mypy app` 통과(168 files) · `TZ=UTC pytest` **274 passed**.
- `alembic upgrade head`/`downgrade -1` PostgreSQL 실DB 검증 통과.
- ruff format `.` 전체는 origin/main에도 있는 기존 baseline drift(67파일)로 실패하나 본 PR 13파일은 정상이며 CI 게이트(`ruff check`)와 무관.

## 보호 파일

`.codex/*`·`docs/decisions/*`·`docs/designs/decision-log-domain.md`는 Opus가 사전 작성(확정본), Codex는 미수정. `alembic/env.py`는 모델 등록 1줄 추가(autogenerate용, 관례). 위반 없음.

## 문서·ADR·실패기록

- ADR-006 동반(스냅샷 자유 JSON·라이프사이클·정본 enum 결정 기록). 추가 ADR 불요.
- `frontend-api-spec.md` decision-logs 섹션 추가(범위 내). `contract-alignment.md` G10은 머지 후 Opus가 후속 갱신 권장.
- 실패기록 불요.

## 비차단 관찰(후속)

- forward-only 전이 미강제(ADR-006 Follow-up): 편집 UI 도입 시 잘못된 역전이 차단 필요.
- 스냅샷 JSON 무검증: 소비측(FE)은 누락/느슨한 필드 허용 가정.
- FE 어댑터 매핑(`ticker↔symbol` 등)은 FE 트랙(FE#48 후속) 범위.
