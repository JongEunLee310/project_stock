# PR #107 로컬 리뷰 — 대시보드 집계 API 신설 (Issue #98)

- 대상: `feat/be-dashboard-summary` → `main`
- 구현: Sonnet 서브에이전트(Agent, general-purpose). 집계 의미는 오케스트레이터(Opus)가 MVP로 정의
- 참조: `contract-alignment.md` G3
- 커밋: `feat: 대시보드 집계 API 신설 (#98)`

## Review Summary

신규 도메인 `app/domains/dashboard/`(service+schema, 테이블 없음)가 기존 alerts/alert_candidates/signals/portfolios repository를 조합해 사용자 단위 집계를 계산. `GET /api/v1/dashboard/summary`(Auth Required), 기존 엔드포인트의 current_user 패턴·공통 envelope 준수. signals에 user_id가 없는 제약을 alerts JOIN으로 우회해 사용자 귀속을 판단한 점 적절. delta 4종은 히스토리 기준선 부재로 null(주석 기재). 스코프 6파일(도메인+router 등록+전용 테스트), 보호 파일 변경 없음. 신규 테스트는 `test_api_contract.py` 미수정·전용 파일 사용으로 충돌 회피.

## Blocking Issues

없음.

## Suggestions

1. **(설계 확인) cash_weight 원가 기준** — 현금 비중을 시장가가 아닌 포지션 **원가 합** 기준으로 계산. 화면이 시장가 기준 비중을 기대하면 어긋남. 가격 시계열(#97)/시세 연동 시 시장가 기준으로 재정의할지 확인 권장(비차단).
2. **(설계 확인) 첫 포트폴리오만** — 다중 포트폴리오 사용자는 첫 포트폴리오의 cash_weight만 반영. 전체 합산이 의도면 후속 조정(비차단).
3. **(후속) delta null** — 히스토리 스냅샷 도입 시 계산. 현재 MVP 계약 형태 고정 목적상 적절.

## Questions

cash_weight 의미(원가 vs 시장가, 단일 vs 합산)는 FE 어댑터(#45)·시세 연동 시점에 확정 필요.

## CI Result

로컬: ruff 통과, mypy 통과(213 files), `TZ=UTC pytest` **225 passed**(신규 8). CI는 PR 생성 후 폴링.

## Documentation Impact

`frontend-api-spec.md`는 #98/#99/#100/#101 통합 갱신 PR로 별도 반영 예정(병렬 PR 간 spec 충돌 회피).

## ADR Need

없음. 확정 계약(G3) 준수, 신규 아키텍처 아님.

## Failure Record Need

없음.

## Final Recommendation

승인 가능(Approve). 차단 이슈 없음. Suggestion 1·2는 시세 연동 시점에 재확인. 최종 승인·머지는 사람이 수행.
