# PR #289 로컬 리뷰 — fix: yfinance earnings_dates에 필요한 lxml 의존성 추가

리뷰어: Claude Code (직접 리뷰 — 의존성 단독 변경으로 VFF 위임 생략)
구현자: Claude Code (orchestrator, 커밋 b44baf3)
날짜: 2026-07-13

## Review Summary

이 PR은 `lxml`을 런타임 의존성으로 추가합니다 (`pyproject.toml` +
`uv.lock`, 코드 변경 없음). PR #288 머지 후 로컬 compose를
`MARKET_PROVIDER=yfinance`로 전환해 백필을 검증하는 과정에서, yfinance
`Ticker.earnings_dates`가 내부적으로 pandas `read_html`을 호출하고 이
함수가 `lxml`을 요구해 `ImportError`로 실적 리포트·발표 이벤트 수집이
전량 빈 결과로 떨어지는 문제를 발견했습니다. mock 환경에서는 이 경로가
실행되지 않아 #285·#287 검증에서 드러나지 않았습니다.

## Blocking

없음.

## Suggestions

없음. lock 파일 diff는 lxml 6.1.1 단일 패키지 추가분만 포함합니다.

## Questions

없음.

## CI Result

`Verify Project Stock` 워크플로 통과. 로컬에서도 `uv run ruff check .`
· `uv run mypy .` · `NEWS_PROVIDER=mock uv run pytest`(790 passed)를
확인했습니다.

## Documentation Impact

설계 문서·ADR 갱신은 필요하지 않습니다. 실 provider 전환 검증에서만
드러나는 의존성 결함이라는 교훈은 quality-process 관점에서 유효하나,
이미 PR #288 리뷰 기록(FastInfo 키 계약)과 같은 맥락으로 기록돼
있습니다.

## Final Recommendation

의존성 선언 추가 단일 변경이며, 호스트 환경 실 스모크 테스트
(`get_quarterly_earnings` 5분기·`get_earnings_events` 25건 반환)와
컨테이너 재빌드 후 실수집 성공으로 효과를 확인했습니다. 머지 준비가
되어 있습니다.

최종 머지 권한은 개발자에게 있습니다.
