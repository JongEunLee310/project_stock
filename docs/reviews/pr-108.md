# PR #108 로컬 리뷰 — frontend-api-spec 통합 갱신 (#98, #99, #100, #101)

- 대상: `feat/be-spec-consolidation` → `main`
- 작성: 오케스트레이터(Opus) 직접. 구현 PR 머지 후 미루었던 spec 통합
- 참조: `contract-alignment.md` G3·G5·G6·G7
- 커밋: `docs: frontend-api-spec에 대시보드·watchlist expand·assets 확장 반영 (#98, #99, #100, #101)`

## Review Summary

docs-only 변경(`docs/api/frontend-api-spec.md` 1파일, +30/-5). 2라운드 BE 구현(#98 대시보드·#99 watchlist expand·#100/#101 assets)은 spec 미수정으로 진행했고(병렬 PR 간 spec 충돌·리뷰 시 필드 변경 가능성 격리), 머지 확정 후 본 PR로 한 번에 문서화. 머지된 실제 스키마/엔드포인트(`dashboard/schema.py`, `watchlists/schema.py`, `assets/schema.py`, 각 endpoint)와 1:1 대조해 작성. 보호 파일·코드 변경 없음.

## Blocking Issues

없음.

## Suggestions

1. **Dashboard `cash_weight` 표기** — spec에 MVP 정의(원가 기준·첫 포트폴리오)와 시세 연동 시 시장가 재정의 예정을 명시. PR #107 리뷰 Suggestion과 일관.
2. **펀더멘털 nullable fallback** — `/detail` 6필드가 mock에서 AAPL만 값. FE 어댑터 `null` 표시 fallback("—") 가이드를 spec에 포함.
3. **watchlist 후보 표** — `expand=asset`는 시세만 합치고 watchlist 상세+items 단일 응답은 아니므로 미구현 후보 행을 유지하되 현재 상태를 괄호로 보강.

## Questions

없음. 확정·머지된 계약을 사후 문서화한 것으로, 의미 결정은 각 구현 PR(#105~107)에서 이미 종료.

## CI Result

CI: `Verify Project Stock` **pass**(run 28023702535, 1m22s). docs-only라 로컬 검증 명령(ruff/mypy/pytest) 해당 없음.

## Documentation Impact

본 PR이 곧 문서 영향분(spec)의 반영물. `contract-alignment.md`(계약 합의 문서)는 변경 불필요 — 합의된 계약 형태와 구현이 일치.

## ADR Need

없음.

## Failure Record Need

없음.

## Final Recommendation

승인 가능(Approve). 코드 변경 없는 사후 문서화. 최종 승인·머지는 사람이 수행.
