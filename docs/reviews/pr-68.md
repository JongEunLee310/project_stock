## Review Summary

PR #68 — Issue #48 (프론트엔드 연동용 API 명세 정리)

핸드오프 범위를 정확히 충족하는 순수 문서/메타데이터 PR이다. `docs/api/frontend-api-spec.md`(687줄)가 신규 추가돼 7개 화면(대시보드/관심종목/종목상세/리서치요약/알림후보/포트폴리오요약/설정)별 API 목록과 도메인별 카탈로그, 미구현 후보, 라우트 체크리스트를 담는다. 응답/에러 예시는 #46/#47 envelope(`{data, message, error, meta}`)를 준수한다. 엔드포인트 변경은 데코레이터 `summary`/`description` 추가뿐으로 시그니처·response_model·동작은 불변임을 diff로 확인했다. 명세 Route Checklist 34개를 OpenAPI 실제 경로와 자동 대조해 `/api/v1` 라우트 전부 1:1 일치를 검증했다. ruff/mypy/pytest 전부 통과. 블로킹 없음, Minor 제안 1건.

---

## Blocking Issues

없음.

---

## Suggestions

### 1. 루트 `GET /health`가 명세·체크리스트에 누락 (Minor)

**위치:** `docs/api/frontend-api-spec.md` Health 섹션 / Route Checklist, `app/main.py`

앱에는 헬스 엔드포인트가 두 개 존재한다 — 라우터 하위 `GET /api/v1/health`와 모니터링용 루트 `GET /health`(envelope 미적용). 본 PR은 `app/main.py`의 루트 `/health`에 OpenAPI `summary`/`description`/`tags`를 추가했으나, 명세 문서와 Route Checklist에는 `/api/v1/health`만 기재돼 루트 `/health`가 빠졌다.

**권장 수정 방향:** Health 섹션에 루트 `GET /health`(envelope 미적용, 모니터링 호환용)를 한 줄 추가하거나, 두 헬스 엔드포인트 중 하나로 정리. 프론트 연동에는 무영향이므로 머지 차단 사유 아님.

---

## Questions

### 1. 헬스 엔드포인트 이원화 의도

루트 `/health`(unwrapped)와 `/api/v1/health`가 공존한다. 모니터링 호환을 위한 의도된 분리로 이해되나(루트는 envelope 제외), 향후 둘 중 하나로 수렴할 계획이 있는지 확인. 본 이슈 범위 밖이므로 답변만 받으면 충분하다.

---

## CI Result

CI 대기 중(`Verify Project Stock` pending). 로컬 검증 결과:

- `uv run ruff check .` — 통과 (All checks passed)
- `uv run mypy .` — 통과 (158 files, no issues)
- `uv run pytest` — 통과 (141 passed)
- 엔드포인트 diff에서 메타데이터(summary/description/tags) 외 변경 없음 확인.
- 문서 Route Checklist ↔ OpenAPI 실제 경로 자동 대조: `/api/v1` 라우트 34개 전부 일치.

---

## Documentation Impact

- `docs/api/frontend-api-spec.md` 신규 — 본 이슈의 핵심 산출물.
- README에 명세 링크 1줄 추가(최소 변경).
- 순수 문서/OpenAPI 메타 PR이므로 설계기록(`docs/designs/`) 생략 타당(design-record-policy "문서 변경" 예외).

ADR 불필요. Failure Record 불필요.

---

## Final Recommendation

**Approve 권장 (사람 최종 승인)** — 핸드오프 범위(화면별 명세 + OpenAPI 메타)를 정확히 충족하고, 코드 동작·스키마·보호 파일 변경이 없으며, 문서가 실제 라우트와 1:1 일치함을 자동 대조로 확인했다. 검증 전부 통과. Minor 제안(루트 `/health` 문서 누락)은 머지 차단 사유 아님.
