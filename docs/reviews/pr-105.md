# PR #105 로컬 리뷰 — assets symbol 필터 + detail 펀더멘털 (Issue #100, #101)

- 대상: `feat/be-assets-symbol-detail` → `main`
- 구현: Sonnet 서브에이전트(Agent, general-purpose)
- 참조: `contract-alignment.md` G6(#100), G7(#101)
- 커밋: `feat: assets symbol 필터 및 detail 펀더멘털 nullable 필드 추가 (#100 #101)`

## Review Summary

- **#100**: `GET /api/v1/assets`에 선택적 `symbol` 필터를 repository/service/endpoint로 전파, 미지정 시 기존 동작·pagination 유지.
- **#101**: `AssetDetailResponse`에 nullable 펀더멘털 6필드(per/peg/52주 고저/목표가/목표수익률) 추가. 값 출처는 market provider(`QuoteResult`)에 6필드를 더해 mock은 AAPL만 채우고 그 외 None. 금액·비율 문자열 Decimal(C5) 일관.

스코프 7파일(market adapter + assets 도메인 + endpoint + 전용 테스트), 보호 파일 변경 없음. 전용 테스트 파일로 `test_api_contract.py` 충돌 회피. 펀더멘털을 provider 계층에 둬 향후 실데이터 전환 지점이 명확.

## Blocking Issues

없음.

## Suggestions

1. **(후속) 펀더멘털 실데이터 출처** — 현재 AAPL 외 None. 실데이터 provider 연동 시 mock 값(31.20 등)이 잔존하지 않도록 ExternalProvider 전환 경로 확인(비차단, 계약 형태 고정이 목적).
2. **(참고) symbol 필터 대소문자** — 정확 일치 필터의 대소문자 정책이 기존 컨벤션과 일치하는지(예: 대문자 정규화) 확인 권장(비차단).

## Questions

없음.

## CI Result

로컬: ruff 통과, mypy 통과(215 files), `TZ=UTC pytest` **239 passed**(신규 7). CI는 PR 생성 후 폴링.

## Documentation Impact

`frontend-api-spec.md`는 #98/#99/#100/#101 통합 갱신 PR로 별도 반영 예정.

## ADR Need

없음. 확정 계약(G6/G7) 준수.

## Failure Record Need

없음.

## Final Recommendation

승인 가능(Approve). 차단 이슈 없음. Suggestion은 실데이터 연동 시점 후속. 최종 승인·머지는 사람이 수행.
