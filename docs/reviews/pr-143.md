# PR #143 로컬 리뷰

리뷰어: Claude Code (Opus, BE 트랙). 작성: Claude Code(문서 변환·정책 직접 작성). 브랜치: docs/adr-korean-and-doc-lang-policy → main.

## Review Summary

영어로 작성돼 있던 ADR 5건(ADR-001/002/004/005/006)의 본문 산문을 한국어로 변환하고, "ADR·설계 문서 본문은 한국어로 작성"한다는 지침을 정책 문서에 명문화합니다. ADR-003은 이미 한국어, ADR-007~009는 #142에서 한국어로 신규 작성됐으므로 남은 영어 문서만 대상입니다.

- 변환 범위: 섹션 헤더(`Status`/`Context`/`Decision`/`Alternatives`/`Consequences`/`Follow-up`/`Related Documents`), Status 라벨(`Accepted`/`Proposed`/`Superseded by ...`), 코드 기호(클래스·enum·파일 경로·플래그·설정 키·식별자), 그리고 결정 내용 자체는 보존하고 산문만 한국어로 옮겼습니다.
- ADR-004의 supersede 경고 블록, ADR-005의 6개 조건, ADR-006의 라이프사이클/스냅샷 결정 등 의미 단위를 1:1로 유지했습니다.
- 지침: `docs/harness/design-record-policy.md`에 Document Language 절을 신설하고, `AGENTS.md` Working Rules와 `CLAUDE.md`에 동일 규칙을 명시하며 design-record-policy를 정본으로 참조하게 했습니다. 기존 `local-review-policy.md`의 리뷰 기록 한국어 규칙과 일관됩니다.

## Blocking Issues

없음.

## Suggestions

**S1 · 보호 파일 변경** — 본 PR은 `docs/decisions/`, `docs/harness/`, `AGENTS.md`, `CLAUDE.md`(모두 AGENTS.md Protected Files)를 수정합니다. 사용자의 명시적 요청에 따른 변경이며, 결정 내용이 아니라 표현 언어와 언어 지침만 바꿉니다. 머지 승인 시 보호 파일 변경임을 확인해 주세요.

## Questions

없음.

## CI Result

문서·정책만 변경(docs/**, AGENTS.md, CLAUDE.md)이라 코드 영향 없음. BE CI 3종 생략.

## Documentation Impact

- ADR 5건 본문 한국어화(결정 불변).
- `design-record-policy.md`에 문서 언어 규칙 신설 → 향후 ADR·설계 문서의 정본 규칙.
- `AGENTS.md`/`CLAUDE.md`에 규칙 반영.

## ADR / Failure Record / Domain Knowledge

- **ADR**: 불요. 기존 결정의 표현 언어 변경이며 새 결정이 아님.
- **실패기록**: 불요.
- **도메인 지식**: 문서 언어 규칙은 메모리(feedback)와 전역 CLAUDE.md에 별도 반영.

## Final Recommendation

**Approve.** 결정 내용을 보존한 채 언어만 한국어로 통일했고, 지침을 정책 문서에 명문화해 재발을 방지합니다. 최종 머지는 사람이 결정합니다.
